/**
 * IPFS Professor
 *
 * a fun rhyming name for a library which checks to see if a certain
 * podcastified video already exists on IPFS.
 *
 */


var research = function research(videoID, cb) {
  // @todo implement
  return cb(null, {isNew: true});
}



module.exports = {
  research: research
}
